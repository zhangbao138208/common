// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.skig.tech/zero-core/common/ent/predicate"
	"gitlab.skig.tech/zero-core/common/ent/wingamelist"
)

// WinGameListQuery is the builder for querying WinGameList entities.
type WinGameListQuery struct {
	config
	ctx        *QueryContext
	order      []wingamelist.OrderOption
	inters     []Interceptor
	predicates []predicate.WinGameList
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WinGameListQuery builder.
func (wglq *WinGameListQuery) Where(ps ...predicate.WinGameList) *WinGameListQuery {
	wglq.predicates = append(wglq.predicates, ps...)
	return wglq
}

// Limit the number of records to be returned by this query.
func (wglq *WinGameListQuery) Limit(limit int) *WinGameListQuery {
	wglq.ctx.Limit = &limit
	return wglq
}

// Offset to start from.
func (wglq *WinGameListQuery) Offset(offset int) *WinGameListQuery {
	wglq.ctx.Offset = &offset
	return wglq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wglq *WinGameListQuery) Unique(unique bool) *WinGameListQuery {
	wglq.ctx.Unique = &unique
	return wglq
}

// Order specifies how the records should be ordered.
func (wglq *WinGameListQuery) Order(o ...wingamelist.OrderOption) *WinGameListQuery {
	wglq.order = append(wglq.order, o...)
	return wglq
}

// First returns the first WinGameList entity from the query.
// Returns a *NotFoundError when no WinGameList was found.
func (wglq *WinGameListQuery) First(ctx context.Context) (*WinGameList, error) {
	nodes, err := wglq.Limit(1).All(setContextOp(ctx, wglq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wingamelist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wglq *WinGameListQuery) FirstX(ctx context.Context) *WinGameList {
	node, err := wglq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WinGameList ID from the query.
// Returns a *NotFoundError when no WinGameList ID was found.
func (wglq *WinGameListQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wglq.Limit(1).IDs(setContextOp(ctx, wglq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wingamelist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wglq *WinGameListQuery) FirstIDX(ctx context.Context) int32 {
	id, err := wglq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WinGameList entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WinGameList entity is found.
// Returns a *NotFoundError when no WinGameList entities are found.
func (wglq *WinGameListQuery) Only(ctx context.Context) (*WinGameList, error) {
	nodes, err := wglq.Limit(2).All(setContextOp(ctx, wglq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wingamelist.Label}
	default:
		return nil, &NotSingularError{wingamelist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wglq *WinGameListQuery) OnlyX(ctx context.Context) *WinGameList {
	node, err := wglq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WinGameList ID in the query.
// Returns a *NotSingularError when more than one WinGameList ID is found.
// Returns a *NotFoundError when no entities are found.
func (wglq *WinGameListQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wglq.Limit(2).IDs(setContextOp(ctx, wglq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wingamelist.Label}
	default:
		err = &NotSingularError{wingamelist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wglq *WinGameListQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := wglq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WinGameLists.
func (wglq *WinGameListQuery) All(ctx context.Context) ([]*WinGameList, error) {
	ctx = setContextOp(ctx, wglq.ctx, "All")
	if err := wglq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WinGameList, *WinGameListQuery]()
	return withInterceptors[[]*WinGameList](ctx, wglq, qr, wglq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wglq *WinGameListQuery) AllX(ctx context.Context) []*WinGameList {
	nodes, err := wglq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WinGameList IDs.
func (wglq *WinGameListQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if wglq.ctx.Unique == nil && wglq.path != nil {
		wglq.Unique(true)
	}
	ctx = setContextOp(ctx, wglq.ctx, "IDs")
	if err = wglq.Select(wingamelist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wglq *WinGameListQuery) IDsX(ctx context.Context) []int32 {
	ids, err := wglq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wglq *WinGameListQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wglq.ctx, "Count")
	if err := wglq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wglq, querierCount[*WinGameListQuery](), wglq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wglq *WinGameListQuery) CountX(ctx context.Context) int {
	count, err := wglq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wglq *WinGameListQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wglq.ctx, "Exist")
	switch _, err := wglq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wglq *WinGameListQuery) ExistX(ctx context.Context) bool {
	exist, err := wglq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WinGameListQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wglq *WinGameListQuery) Clone() *WinGameListQuery {
	if wglq == nil {
		return nil
	}
	return &WinGameListQuery{
		config:     wglq.config,
		ctx:        wglq.ctx.Clone(),
		order:      append([]wingamelist.OrderOption{}, wglq.order...),
		inters:     append([]Interceptor{}, wglq.inters...),
		predicates: append([]predicate.WinGameList{}, wglq.predicates...),
		// clone intermediate query.
		sql:  wglq.sql.Clone(),
		path: wglq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WinGameList.Query().
//		GroupBy(wingamelist.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wglq *WinGameListQuery) GroupBy(field string, fields ...string) *WinGameListGroupBy {
	wglq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WinGameListGroupBy{build: wglq}
	grbuild.flds = &wglq.ctx.Fields
	grbuild.label = wingamelist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.WinGameList.Query().
//		Select(wingamelist.FieldCode).
//		Scan(ctx, &v)
func (wglq *WinGameListQuery) Select(fields ...string) *WinGameListSelect {
	wglq.ctx.Fields = append(wglq.ctx.Fields, fields...)
	sbuild := &WinGameListSelect{WinGameListQuery: wglq}
	sbuild.label = wingamelist.Label
	sbuild.flds, sbuild.scan = &wglq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WinGameListSelect configured with the given aggregations.
func (wglq *WinGameListQuery) Aggregate(fns ...AggregateFunc) *WinGameListSelect {
	return wglq.Select().Aggregate(fns...)
}

func (wglq *WinGameListQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wglq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wglq); err != nil {
				return err
			}
		}
	}
	for _, f := range wglq.ctx.Fields {
		if !wingamelist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wglq.path != nil {
		prev, err := wglq.path(ctx)
		if err != nil {
			return err
		}
		wglq.sql = prev
	}
	return nil
}

func (wglq *WinGameListQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WinGameList, error) {
	var (
		nodes = []*WinGameList{}
		_spec = wglq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WinGameList).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WinGameList{config: wglq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wglq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wglq *WinGameListQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wglq.querySpec()
	_spec.Node.Columns = wglq.ctx.Fields
	if len(wglq.ctx.Fields) > 0 {
		_spec.Unique = wglq.ctx.Unique != nil && *wglq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wglq.driver, _spec)
}

func (wglq *WinGameListQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wingamelist.Table, wingamelist.Columns, sqlgraph.NewFieldSpec(wingamelist.FieldID, field.TypeInt32))
	_spec.From = wglq.sql
	if unique := wglq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wglq.path != nil {
		_spec.Unique = true
	}
	if fields := wglq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wingamelist.FieldID)
		for i := range fields {
			if fields[i] != wingamelist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wglq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wglq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wglq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wglq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wglq *WinGameListQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wglq.driver.Dialect())
	t1 := builder.Table(wingamelist.Table)
	columns := wglq.ctx.Fields
	if len(columns) == 0 {
		columns = wingamelist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wglq.sql != nil {
		selector = wglq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wglq.ctx.Unique != nil && *wglq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wglq.predicates {
		p(selector)
	}
	for _, p := range wglq.order {
		p(selector)
	}
	if offset := wglq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wglq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

func (wglq *WinGameListQuery) SqlQuery(ctx context.Context) (r1 string, r2 []any) {
	builder := sql.Dialect(wglq.driver.Dialect())
	t1 := builder.Table(wingamelist.Table)
	columns := wglq.ctx.Fields
	if len(columns) == 0 {
		columns = wingamelist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wglq.sql != nil {
		selector = wglq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wglq.ctx.Unique != nil && *wglq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wglq.predicates {
		p(selector)
	}
	for _, p := range wglq.order {
		p(selector)
	}
	if offset := wglq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wglq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	r1, r2 = selector.Query()
	//r3 = make([]*WinGameList,0)

	return
}

// WinGameListGroupBy is the group-by builder for WinGameList entities.
type WinGameListGroupBy struct {
	selector
	build *WinGameListQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wglgb *WinGameListGroupBy) Aggregate(fns ...AggregateFunc) *WinGameListGroupBy {
	wglgb.fns = append(wglgb.fns, fns...)
	return wglgb
}

// Scan applies the selector query and scans the result into the given value.
func (wglgb *WinGameListGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wglgb.build.ctx, "GroupBy")
	if err := wglgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinGameListQuery, *WinGameListGroupBy](ctx, wglgb.build, wglgb, wglgb.build.inters, v)
}

func (wglgb *WinGameListGroupBy) sqlScan(ctx context.Context, root *WinGameListQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wglgb.fns))
	for _, fn := range wglgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wglgb.flds)+len(wglgb.fns))
		for _, f := range *wglgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wglgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wglgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WinGameListSelect is the builder for selecting fields of WinGameList entities.
type WinGameListSelect struct {
	*WinGameListQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wgls *WinGameListSelect) Aggregate(fns ...AggregateFunc) *WinGameListSelect {
	wgls.fns = append(wgls.fns, fns...)
	return wgls
}

// Scan applies the selector query and scans the result into the given value.
func (wgls *WinGameListSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wgls.ctx, "Select")
	if err := wgls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinGameListQuery, *WinGameListSelect](ctx, wgls.WinGameListQuery, wgls, wgls.inters, v)
}

func (wgls *WinGameListSelect) sqlScan(ctx context.Context, root *WinGameListQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wgls.fns))
	for _, fn := range wgls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wgls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wgls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
