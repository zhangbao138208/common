// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.skig.tech/zero-core/common/ent/predicate"
	"gitlab.skig.tech/zero-core/common/ent/windictitem"
)

// WinDictItemQuery is the builder for querying WinDictItem entities.
type WinDictItemQuery struct {
	config
	ctx        *QueryContext
	order      []windictitem.OrderOption
	inters     []Interceptor
	predicates []predicate.WinDictItem
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WinDictItemQuery builder.
func (wdiq *WinDictItemQuery) Where(ps ...predicate.WinDictItem) *WinDictItemQuery {
	wdiq.predicates = append(wdiq.predicates, ps...)
	return wdiq
}

// Limit the number of records to be returned by this query.
func (wdiq *WinDictItemQuery) Limit(limit int) *WinDictItemQuery {
	wdiq.ctx.Limit = &limit
	return wdiq
}

// Offset to start from.
func (wdiq *WinDictItemQuery) Offset(offset int) *WinDictItemQuery {
	wdiq.ctx.Offset = &offset
	return wdiq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wdiq *WinDictItemQuery) Unique(unique bool) *WinDictItemQuery {
	wdiq.ctx.Unique = &unique
	return wdiq
}

// Order specifies how the records should be ordered.
func (wdiq *WinDictItemQuery) Order(o ...windictitem.OrderOption) *WinDictItemQuery {
	wdiq.order = append(wdiq.order, o...)
	return wdiq
}

// First returns the first WinDictItem entity from the query.
// Returns a *NotFoundError when no WinDictItem was found.
func (wdiq *WinDictItemQuery) First(ctx context.Context) (*WinDictItem, error) {
	nodes, err := wdiq.Limit(1).All(setContextOp(ctx, wdiq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{windictitem.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wdiq *WinDictItemQuery) FirstX(ctx context.Context) *WinDictItem {
	node, err := wdiq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WinDictItem ID from the query.
// Returns a *NotFoundError when no WinDictItem ID was found.
func (wdiq *WinDictItemQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wdiq.Limit(1).IDs(setContextOp(ctx, wdiq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{windictitem.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wdiq *WinDictItemQuery) FirstIDX(ctx context.Context) int32 {
	id, err := wdiq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WinDictItem entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WinDictItem entity is found.
// Returns a *NotFoundError when no WinDictItem entities are found.
func (wdiq *WinDictItemQuery) Only(ctx context.Context) (*WinDictItem, error) {
	nodes, err := wdiq.Limit(2).All(setContextOp(ctx, wdiq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{windictitem.Label}
	default:
		return nil, &NotSingularError{windictitem.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wdiq *WinDictItemQuery) OnlyX(ctx context.Context) *WinDictItem {
	node, err := wdiq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WinDictItem ID in the query.
// Returns a *NotSingularError when more than one WinDictItem ID is found.
// Returns a *NotFoundError when no entities are found.
func (wdiq *WinDictItemQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wdiq.Limit(2).IDs(setContextOp(ctx, wdiq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{windictitem.Label}
	default:
		err = &NotSingularError{windictitem.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wdiq *WinDictItemQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := wdiq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WinDictItems.
func (wdiq *WinDictItemQuery) All(ctx context.Context) ([]*WinDictItem, error) {
	ctx = setContextOp(ctx, wdiq.ctx, "All")
	if err := wdiq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WinDictItem, *WinDictItemQuery]()
	return withInterceptors[[]*WinDictItem](ctx, wdiq, qr, wdiq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wdiq *WinDictItemQuery) AllX(ctx context.Context) []*WinDictItem {
	nodes, err := wdiq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WinDictItem IDs.
func (wdiq *WinDictItemQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if wdiq.ctx.Unique == nil && wdiq.path != nil {
		wdiq.Unique(true)
	}
	ctx = setContextOp(ctx, wdiq.ctx, "IDs")
	if err = wdiq.Select(windictitem.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wdiq *WinDictItemQuery) IDsX(ctx context.Context) []int32 {
	ids, err := wdiq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wdiq *WinDictItemQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wdiq.ctx, "Count")
	if err := wdiq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wdiq, querierCount[*WinDictItemQuery](), wdiq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wdiq *WinDictItemQuery) CountX(ctx context.Context) int {
	count, err := wdiq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wdiq *WinDictItemQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wdiq.ctx, "Exist")
	switch _, err := wdiq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wdiq *WinDictItemQuery) ExistX(ctx context.Context) bool {
	exist, err := wdiq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WinDictItemQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wdiq *WinDictItemQuery) Clone() *WinDictItemQuery {
	if wdiq == nil {
		return nil
	}
	return &WinDictItemQuery{
		config:     wdiq.config,
		ctx:        wdiq.ctx.Clone(),
		order:      append([]windictitem.OrderOption{}, wdiq.order...),
		inters:     append([]Interceptor{}, wdiq.inters...),
		predicates: append([]predicate.WinDictItem{}, wdiq.predicates...),
		// clone intermediate query.
		sql:  wdiq.sql.Clone(),
		path: wdiq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WinDictItem.Query().
//		GroupBy(windictitem.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wdiq *WinDictItemQuery) GroupBy(field string, fields ...string) *WinDictItemGroupBy {
	wdiq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WinDictItemGroupBy{build: wdiq}
	grbuild.flds = &wdiq.ctx.Fields
	grbuild.label = windictitem.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.WinDictItem.Query().
//		Select(windictitem.FieldCode).
//		Scan(ctx, &v)
func (wdiq *WinDictItemQuery) Select(fields ...string) *WinDictItemSelect {
	wdiq.ctx.Fields = append(wdiq.ctx.Fields, fields...)
	sbuild := &WinDictItemSelect{WinDictItemQuery: wdiq}
	sbuild.label = windictitem.Label
	sbuild.flds, sbuild.scan = &wdiq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WinDictItemSelect configured with the given aggregations.
func (wdiq *WinDictItemQuery) Aggregate(fns ...AggregateFunc) *WinDictItemSelect {
	return wdiq.Select().Aggregate(fns...)
}

func (wdiq *WinDictItemQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wdiq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wdiq); err != nil {
				return err
			}
		}
	}
	for _, f := range wdiq.ctx.Fields {
		if !windictitem.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wdiq.path != nil {
		prev, err := wdiq.path(ctx)
		if err != nil {
			return err
		}
		wdiq.sql = prev
	}
	return nil
}

func (wdiq *WinDictItemQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WinDictItem, error) {
	var (
		nodes = []*WinDictItem{}
		_spec = wdiq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WinDictItem).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WinDictItem{config: wdiq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wdiq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wdiq *WinDictItemQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wdiq.querySpec()
	_spec.Node.Columns = wdiq.ctx.Fields
	if len(wdiq.ctx.Fields) > 0 {
		_spec.Unique = wdiq.ctx.Unique != nil && *wdiq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wdiq.driver, _spec)
}

func (wdiq *WinDictItemQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(windictitem.Table, windictitem.Columns, sqlgraph.NewFieldSpec(windictitem.FieldID, field.TypeInt32))
	_spec.From = wdiq.sql
	if unique := wdiq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wdiq.path != nil {
		_spec.Unique = true
	}
	if fields := wdiq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, windictitem.FieldID)
		for i := range fields {
			if fields[i] != windictitem.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wdiq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wdiq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wdiq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wdiq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wdiq *WinDictItemQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wdiq.driver.Dialect())
	t1 := builder.Table(windictitem.Table)
	columns := wdiq.ctx.Fields
	if len(columns) == 0 {
		columns = windictitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wdiq.sql != nil {
		selector = wdiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wdiq.ctx.Unique != nil && *wdiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wdiq.predicates {
		p(selector)
	}
	for _, p := range wdiq.order {
		p(selector)
	}
	if offset := wdiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wdiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

func (wdiq *WinDictItemQuery) SqlQuery(ctx context.Context) (r1 string, r2 []any) {
	builder := sql.Dialect(wdiq.driver.Dialect())
	t1 := builder.Table(windictitem.Table)
	columns := wdiq.ctx.Fields
	if len(columns) == 0 {
		columns = windictitem.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wdiq.sql != nil {
		selector = wdiq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wdiq.ctx.Unique != nil && *wdiq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wdiq.predicates {
		p(selector)
	}
	for _, p := range wdiq.order {
		p(selector)
	}
	if offset := wdiq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wdiq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	r1, r2 = selector.Query()
	//r3 = make([]*WinDictItem,0)

	return
}

// WinDictItemGroupBy is the group-by builder for WinDictItem entities.
type WinDictItemGroupBy struct {
	selector
	build *WinDictItemQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wdigb *WinDictItemGroupBy) Aggregate(fns ...AggregateFunc) *WinDictItemGroupBy {
	wdigb.fns = append(wdigb.fns, fns...)
	return wdigb
}

// Scan applies the selector query and scans the result into the given value.
func (wdigb *WinDictItemGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wdigb.build.ctx, "GroupBy")
	if err := wdigb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinDictItemQuery, *WinDictItemGroupBy](ctx, wdigb.build, wdigb, wdigb.build.inters, v)
}

func (wdigb *WinDictItemGroupBy) sqlScan(ctx context.Context, root *WinDictItemQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wdigb.fns))
	for _, fn := range wdigb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wdigb.flds)+len(wdigb.fns))
		for _, f := range *wdigb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wdigb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wdigb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WinDictItemSelect is the builder for selecting fields of WinDictItem entities.
type WinDictItemSelect struct {
	*WinDictItemQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wdis *WinDictItemSelect) Aggregate(fns ...AggregateFunc) *WinDictItemSelect {
	wdis.fns = append(wdis.fns, fns...)
	return wdis
}

// Scan applies the selector query and scans the result into the given value.
func (wdis *WinDictItemSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wdis.ctx, "Select")
	if err := wdis.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinDictItemQuery, *WinDictItemSelect](ctx, wdis.WinDictItemQuery, wdis, wdis.inters, v)
}

func (wdis *WinDictItemSelect) sqlScan(ctx context.Context, root *WinDictItemQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wdis.fns))
	for _, fn := range wdis.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wdis.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wdis.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
