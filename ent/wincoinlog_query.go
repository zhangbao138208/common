// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.skig.tech/zero-core/common/ent/predicate"
	"gitlab.skig.tech/zero-core/common/ent/wincoinlog"
)

// WinCoinLogQuery is the builder for querying WinCoinLog entities.
type WinCoinLogQuery struct {
	config
	ctx        *QueryContext
	order      []wincoinlog.OrderOption
	inters     []Interceptor
	predicates []predicate.WinCoinLog
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WinCoinLogQuery builder.
func (wclq *WinCoinLogQuery) Where(ps ...predicate.WinCoinLog) *WinCoinLogQuery {
	wclq.predicates = append(wclq.predicates, ps...)
	return wclq
}

// Limit the number of records to be returned by this query.
func (wclq *WinCoinLogQuery) Limit(limit int) *WinCoinLogQuery {
	wclq.ctx.Limit = &limit
	return wclq
}

// Offset to start from.
func (wclq *WinCoinLogQuery) Offset(offset int) *WinCoinLogQuery {
	wclq.ctx.Offset = &offset
	return wclq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wclq *WinCoinLogQuery) Unique(unique bool) *WinCoinLogQuery {
	wclq.ctx.Unique = &unique
	return wclq
}

// Order specifies how the records should be ordered.
func (wclq *WinCoinLogQuery) Order(o ...wincoinlog.OrderOption) *WinCoinLogQuery {
	wclq.order = append(wclq.order, o...)
	return wclq
}

// First returns the first WinCoinLog entity from the query.
// Returns a *NotFoundError when no WinCoinLog was found.
func (wclq *WinCoinLogQuery) First(ctx context.Context) (*WinCoinLog, error) {
	nodes, err := wclq.Limit(1).All(setContextOp(ctx, wclq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{wincoinlog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wclq *WinCoinLogQuery) FirstX(ctx context.Context) *WinCoinLog {
	node, err := wclq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WinCoinLog ID from the query.
// Returns a *NotFoundError when no WinCoinLog ID was found.
func (wclq *WinCoinLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wclq.Limit(1).IDs(setContextOp(ctx, wclq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{wincoinlog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wclq *WinCoinLogQuery) FirstIDX(ctx context.Context) int {
	id, err := wclq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WinCoinLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WinCoinLog entity is found.
// Returns a *NotFoundError when no WinCoinLog entities are found.
func (wclq *WinCoinLogQuery) Only(ctx context.Context) (*WinCoinLog, error) {
	nodes, err := wclq.Limit(2).All(setContextOp(ctx, wclq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{wincoinlog.Label}
	default:
		return nil, &NotSingularError{wincoinlog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wclq *WinCoinLogQuery) OnlyX(ctx context.Context) *WinCoinLog {
	node, err := wclq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WinCoinLog ID in the query.
// Returns a *NotSingularError when more than one WinCoinLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (wclq *WinCoinLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = wclq.Limit(2).IDs(setContextOp(ctx, wclq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{wincoinlog.Label}
	default:
		err = &NotSingularError{wincoinlog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wclq *WinCoinLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := wclq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WinCoinLogs.
func (wclq *WinCoinLogQuery) All(ctx context.Context) ([]*WinCoinLog, error) {
	ctx = setContextOp(ctx, wclq.ctx, "All")
	if err := wclq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WinCoinLog, *WinCoinLogQuery]()
	return withInterceptors[[]*WinCoinLog](ctx, wclq, qr, wclq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wclq *WinCoinLogQuery) AllX(ctx context.Context) []*WinCoinLog {
	nodes, err := wclq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WinCoinLog IDs.
func (wclq *WinCoinLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if wclq.ctx.Unique == nil && wclq.path != nil {
		wclq.Unique(true)
	}
	ctx = setContextOp(ctx, wclq.ctx, "IDs")
	if err = wclq.Select(wincoinlog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wclq *WinCoinLogQuery) IDsX(ctx context.Context) []int {
	ids, err := wclq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wclq *WinCoinLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wclq.ctx, "Count")
	if err := wclq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wclq, querierCount[*WinCoinLogQuery](), wclq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wclq *WinCoinLogQuery) CountX(ctx context.Context) int {
	count, err := wclq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wclq *WinCoinLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wclq.ctx, "Exist")
	switch _, err := wclq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wclq *WinCoinLogQuery) ExistX(ctx context.Context) bool {
	exist, err := wclq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WinCoinLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wclq *WinCoinLogQuery) Clone() *WinCoinLogQuery {
	if wclq == nil {
		return nil
	}
	return &WinCoinLogQuery{
		config:     wclq.config,
		ctx:        wclq.ctx.Clone(),
		order:      append([]wincoinlog.OrderOption{}, wclq.order...),
		inters:     append([]Interceptor{}, wclq.inters...),
		predicates: append([]predicate.WinCoinLog{}, wclq.predicates...),
		// clone intermediate query.
		sql:  wclq.sql.Clone(),
		path: wclq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UID int32 `json:"uid,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WinCoinLog.Query().
//		GroupBy(wincoinlog.FieldUID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wclq *WinCoinLogQuery) GroupBy(field string, fields ...string) *WinCoinLogGroupBy {
	wclq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WinCoinLogGroupBy{build: wclq}
	grbuild.flds = &wclq.ctx.Fields
	grbuild.label = wincoinlog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UID int32 `json:"uid,omitempty"`
//	}
//
//	client.WinCoinLog.Query().
//		Select(wincoinlog.FieldUID).
//		Scan(ctx, &v)
func (wclq *WinCoinLogQuery) Select(fields ...string) *WinCoinLogSelect {
	wclq.ctx.Fields = append(wclq.ctx.Fields, fields...)
	sbuild := &WinCoinLogSelect{WinCoinLogQuery: wclq}
	sbuild.label = wincoinlog.Label
	sbuild.flds, sbuild.scan = &wclq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WinCoinLogSelect configured with the given aggregations.
func (wclq *WinCoinLogQuery) Aggregate(fns ...AggregateFunc) *WinCoinLogSelect {
	return wclq.Select().Aggregate(fns...)
}

func (wclq *WinCoinLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wclq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wclq); err != nil {
				return err
			}
		}
	}
	for _, f := range wclq.ctx.Fields {
		if !wincoinlog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wclq.path != nil {
		prev, err := wclq.path(ctx)
		if err != nil {
			return err
		}
		wclq.sql = prev
	}
	return nil
}

func (wclq *WinCoinLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WinCoinLog, error) {
	var (
		nodes = []*WinCoinLog{}
		_spec = wclq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WinCoinLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WinCoinLog{config: wclq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wclq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wclq *WinCoinLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wclq.querySpec()
	_spec.Node.Columns = wclq.ctx.Fields
	if len(wclq.ctx.Fields) > 0 {
		_spec.Unique = wclq.ctx.Unique != nil && *wclq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wclq.driver, _spec)
}

func (wclq *WinCoinLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(wincoinlog.Table, wincoinlog.Columns, sqlgraph.NewFieldSpec(wincoinlog.FieldID, field.TypeInt))
	_spec.From = wclq.sql
	if unique := wclq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wclq.path != nil {
		_spec.Unique = true
	}
	if fields := wclq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, wincoinlog.FieldID)
		for i := range fields {
			if fields[i] != wincoinlog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wclq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wclq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wclq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wclq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wclq *WinCoinLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wclq.driver.Dialect())
	t1 := builder.Table(wincoinlog.Table)
	columns := wclq.ctx.Fields
	if len(columns) == 0 {
		columns = wincoinlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wclq.sql != nil {
		selector = wclq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wclq.ctx.Unique != nil && *wclq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wclq.predicates {
		p(selector)
	}
	for _, p := range wclq.order {
		p(selector)
	}
	if offset := wclq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wclq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

func (wclq *WinCoinLogQuery) SqlQuery(ctx context.Context) (r1 string, r2 []any) {
	builder := sql.Dialect(wclq.driver.Dialect())
	t1 := builder.Table(wincoinlog.Table)
	columns := wclq.ctx.Fields
	if len(columns) == 0 {
		columns = wincoinlog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wclq.sql != nil {
		selector = wclq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wclq.ctx.Unique != nil && *wclq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wclq.predicates {
		p(selector)
	}
	for _, p := range wclq.order {
		p(selector)
	}
	if offset := wclq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wclq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	r1, r2 = selector.Query()
	//r3 = make([]*WinCoinLog,0)

	return
}

// WinCoinLogGroupBy is the group-by builder for WinCoinLog entities.
type WinCoinLogGroupBy struct {
	selector
	build *WinCoinLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wclgb *WinCoinLogGroupBy) Aggregate(fns ...AggregateFunc) *WinCoinLogGroupBy {
	wclgb.fns = append(wclgb.fns, fns...)
	return wclgb
}

// Scan applies the selector query and scans the result into the given value.
func (wclgb *WinCoinLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wclgb.build.ctx, "GroupBy")
	if err := wclgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinCoinLogQuery, *WinCoinLogGroupBy](ctx, wclgb.build, wclgb, wclgb.build.inters, v)
}

func (wclgb *WinCoinLogGroupBy) sqlScan(ctx context.Context, root *WinCoinLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wclgb.fns))
	for _, fn := range wclgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wclgb.flds)+len(wclgb.fns))
		for _, f := range *wclgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wclgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wclgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WinCoinLogSelect is the builder for selecting fields of WinCoinLog entities.
type WinCoinLogSelect struct {
	*WinCoinLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wcls *WinCoinLogSelect) Aggregate(fns ...AggregateFunc) *WinCoinLogSelect {
	wcls.fns = append(wcls.fns, fns...)
	return wcls
}

// Scan applies the selector query and scans the result into the given value.
func (wcls *WinCoinLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wcls.ctx, "Select")
	if err := wcls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinCoinLogQuery, *WinCoinLogSelect](ctx, wcls.WinCoinLogQuery, wcls, wcls.inters, v)
}

func (wcls *WinCoinLogSelect) sqlScan(ctx context.Context, root *WinCoinLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wcls.fns))
	for _, fn := range wcls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wcls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wcls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
