// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.skig.tech/zero-core/common/ent/predicate"
	"gitlab.skig.tech/zero-core/common/ent/winplatlist"
)

// WinPlatListQuery is the builder for querying WinPlatList entities.
type WinPlatListQuery struct {
	config
	ctx        *QueryContext
	order      []winplatlist.OrderOption
	inters     []Interceptor
	predicates []predicate.WinPlatList
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WinPlatListQuery builder.
func (wplq *WinPlatListQuery) Where(ps ...predicate.WinPlatList) *WinPlatListQuery {
	wplq.predicates = append(wplq.predicates, ps...)
	return wplq
}

// Limit the number of records to be returned by this query.
func (wplq *WinPlatListQuery) Limit(limit int) *WinPlatListQuery {
	wplq.ctx.Limit = &limit
	return wplq
}

// Offset to start from.
func (wplq *WinPlatListQuery) Offset(offset int) *WinPlatListQuery {
	wplq.ctx.Offset = &offset
	return wplq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wplq *WinPlatListQuery) Unique(unique bool) *WinPlatListQuery {
	wplq.ctx.Unique = &unique
	return wplq
}

// Order specifies how the records should be ordered.
func (wplq *WinPlatListQuery) Order(o ...winplatlist.OrderOption) *WinPlatListQuery {
	wplq.order = append(wplq.order, o...)
	return wplq
}

// First returns the first WinPlatList entity from the query.
// Returns a *NotFoundError when no WinPlatList was found.
func (wplq *WinPlatListQuery) First(ctx context.Context) (*WinPlatList, error) {
	nodes, err := wplq.Limit(1).All(setContextOp(ctx, wplq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{winplatlist.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wplq *WinPlatListQuery) FirstX(ctx context.Context) *WinPlatList {
	node, err := wplq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WinPlatList ID from the query.
// Returns a *NotFoundError when no WinPlatList ID was found.
func (wplq *WinPlatListQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wplq.Limit(1).IDs(setContextOp(ctx, wplq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{winplatlist.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wplq *WinPlatListQuery) FirstIDX(ctx context.Context) int32 {
	id, err := wplq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WinPlatList entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WinPlatList entity is found.
// Returns a *NotFoundError when no WinPlatList entities are found.
func (wplq *WinPlatListQuery) Only(ctx context.Context) (*WinPlatList, error) {
	nodes, err := wplq.Limit(2).All(setContextOp(ctx, wplq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{winplatlist.Label}
	default:
		return nil, &NotSingularError{winplatlist.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wplq *WinPlatListQuery) OnlyX(ctx context.Context) *WinPlatList {
	node, err := wplq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WinPlatList ID in the query.
// Returns a *NotSingularError when more than one WinPlatList ID is found.
// Returns a *NotFoundError when no entities are found.
func (wplq *WinPlatListQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wplq.Limit(2).IDs(setContextOp(ctx, wplq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{winplatlist.Label}
	default:
		err = &NotSingularError{winplatlist.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wplq *WinPlatListQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := wplq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WinPlatLists.
func (wplq *WinPlatListQuery) All(ctx context.Context) ([]*WinPlatList, error) {
	ctx = setContextOp(ctx, wplq.ctx, "All")
	if err := wplq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WinPlatList, *WinPlatListQuery]()
	return withInterceptors[[]*WinPlatList](ctx, wplq, qr, wplq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wplq *WinPlatListQuery) AllX(ctx context.Context) []*WinPlatList {
	nodes, err := wplq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WinPlatList IDs.
func (wplq *WinPlatListQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if wplq.ctx.Unique == nil && wplq.path != nil {
		wplq.Unique(true)
	}
	ctx = setContextOp(ctx, wplq.ctx, "IDs")
	if err = wplq.Select(winplatlist.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wplq *WinPlatListQuery) IDsX(ctx context.Context) []int32 {
	ids, err := wplq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wplq *WinPlatListQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wplq.ctx, "Count")
	if err := wplq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wplq, querierCount[*WinPlatListQuery](), wplq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wplq *WinPlatListQuery) CountX(ctx context.Context) int {
	count, err := wplq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wplq *WinPlatListQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wplq.ctx, "Exist")
	switch _, err := wplq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wplq *WinPlatListQuery) ExistX(ctx context.Context) bool {
	exist, err := wplq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WinPlatListQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wplq *WinPlatListQuery) Clone() *WinPlatListQuery {
	if wplq == nil {
		return nil
	}
	return &WinPlatListQuery{
		config:     wplq.config,
		ctx:        wplq.ctx.Clone(),
		order:      append([]winplatlist.OrderOption{}, wplq.order...),
		inters:     append([]Interceptor{}, wplq.inters...),
		predicates: append([]predicate.WinPlatList{}, wplq.predicates...),
		// clone intermediate query.
		sql:  wplq.sql.Clone(),
		path: wplq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WinPlatList.Query().
//		GroupBy(winplatlist.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wplq *WinPlatListQuery) GroupBy(field string, fields ...string) *WinPlatListGroupBy {
	wplq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WinPlatListGroupBy{build: wplq}
	grbuild.flds = &wplq.ctx.Fields
	grbuild.label = winplatlist.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.WinPlatList.Query().
//		Select(winplatlist.FieldCode).
//		Scan(ctx, &v)
func (wplq *WinPlatListQuery) Select(fields ...string) *WinPlatListSelect {
	wplq.ctx.Fields = append(wplq.ctx.Fields, fields...)
	sbuild := &WinPlatListSelect{WinPlatListQuery: wplq}
	sbuild.label = winplatlist.Label
	sbuild.flds, sbuild.scan = &wplq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WinPlatListSelect configured with the given aggregations.
func (wplq *WinPlatListQuery) Aggregate(fns ...AggregateFunc) *WinPlatListSelect {
	return wplq.Select().Aggregate(fns...)
}

func (wplq *WinPlatListQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wplq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wplq); err != nil {
				return err
			}
		}
	}
	for _, f := range wplq.ctx.Fields {
		if !winplatlist.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wplq.path != nil {
		prev, err := wplq.path(ctx)
		if err != nil {
			return err
		}
		wplq.sql = prev
	}
	return nil
}

func (wplq *WinPlatListQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WinPlatList, error) {
	var (
		nodes = []*WinPlatList{}
		_spec = wplq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WinPlatList).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WinPlatList{config: wplq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wplq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wplq *WinPlatListQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wplq.querySpec()
	_spec.Node.Columns = wplq.ctx.Fields
	if len(wplq.ctx.Fields) > 0 {
		_spec.Unique = wplq.ctx.Unique != nil && *wplq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wplq.driver, _spec)
}

func (wplq *WinPlatListQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(winplatlist.Table, winplatlist.Columns, sqlgraph.NewFieldSpec(winplatlist.FieldID, field.TypeInt32))
	_spec.From = wplq.sql
	if unique := wplq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wplq.path != nil {
		_spec.Unique = true
	}
	if fields := wplq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, winplatlist.FieldID)
		for i := range fields {
			if fields[i] != winplatlist.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wplq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wplq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wplq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wplq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wplq *WinPlatListQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wplq.driver.Dialect())
	t1 := builder.Table(winplatlist.Table)
	columns := wplq.ctx.Fields
	if len(columns) == 0 {
		columns = winplatlist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wplq.sql != nil {
		selector = wplq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wplq.ctx.Unique != nil && *wplq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wplq.predicates {
		p(selector)
	}
	for _, p := range wplq.order {
		p(selector)
	}
	if offset := wplq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wplq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

func (wplq *WinPlatListQuery) SqlQuery(ctx context.Context) (r1 string, r2 []any) {
	builder := sql.Dialect(wplq.driver.Dialect())
	t1 := builder.Table(winplatlist.Table)
	columns := wplq.ctx.Fields
	if len(columns) == 0 {
		columns = winplatlist.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wplq.sql != nil {
		selector = wplq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wplq.ctx.Unique != nil && *wplq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wplq.predicates {
		p(selector)
	}
	for _, p := range wplq.order {
		p(selector)
	}
	if offset := wplq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wplq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	r1, r2 = selector.Query()
	//r3 = make([]*WinPlatList,0)

	return
}

// WinPlatListGroupBy is the group-by builder for WinPlatList entities.
type WinPlatListGroupBy struct {
	selector
	build *WinPlatListQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wplgb *WinPlatListGroupBy) Aggregate(fns ...AggregateFunc) *WinPlatListGroupBy {
	wplgb.fns = append(wplgb.fns, fns...)
	return wplgb
}

// Scan applies the selector query and scans the result into the given value.
func (wplgb *WinPlatListGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wplgb.build.ctx, "GroupBy")
	if err := wplgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinPlatListQuery, *WinPlatListGroupBy](ctx, wplgb.build, wplgb, wplgb.build.inters, v)
}

func (wplgb *WinPlatListGroupBy) sqlScan(ctx context.Context, root *WinPlatListQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wplgb.fns))
	for _, fn := range wplgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wplgb.flds)+len(wplgb.fns))
		for _, f := range *wplgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wplgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wplgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WinPlatListSelect is the builder for selecting fields of WinPlatList entities.
type WinPlatListSelect struct {
	*WinPlatListQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wpls *WinPlatListSelect) Aggregate(fns ...AggregateFunc) *WinPlatListSelect {
	wpls.fns = append(wpls.fns, fns...)
	return wpls
}

// Scan applies the selector query and scans the result into the given value.
func (wpls *WinPlatListSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wpls.ctx, "Select")
	if err := wpls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinPlatListQuery, *WinPlatListSelect](ctx, wpls.WinPlatListQuery, wpls, wpls.inters, v)
}

func (wpls *WinPlatListSelect) sqlScan(ctx context.Context, root *WinPlatListQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wpls.fns))
	for _, fn := range wpls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wpls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wpls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
