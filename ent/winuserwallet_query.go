// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"gitlab.skig.tech/zero-core/common/ent/predicate"
	"gitlab.skig.tech/zero-core/common/ent/winuserwallet"
)

// WinUserWalletQuery is the builder for querying WinUserWallet entities.
type WinUserWalletQuery struct {
	config
	ctx        *QueryContext
	order      []winuserwallet.OrderOption
	inters     []Interceptor
	predicates []predicate.WinUserWallet
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the WinUserWalletQuery builder.
func (wuwq *WinUserWalletQuery) Where(ps ...predicate.WinUserWallet) *WinUserWalletQuery {
	wuwq.predicates = append(wuwq.predicates, ps...)
	return wuwq
}

// Limit the number of records to be returned by this query.
func (wuwq *WinUserWalletQuery) Limit(limit int) *WinUserWalletQuery {
	wuwq.ctx.Limit = &limit
	return wuwq
}

// Offset to start from.
func (wuwq *WinUserWalletQuery) Offset(offset int) *WinUserWalletQuery {
	wuwq.ctx.Offset = &offset
	return wuwq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (wuwq *WinUserWalletQuery) Unique(unique bool) *WinUserWalletQuery {
	wuwq.ctx.Unique = &unique
	return wuwq
}

// Order specifies how the records should be ordered.
func (wuwq *WinUserWalletQuery) Order(o ...winuserwallet.OrderOption) *WinUserWalletQuery {
	wuwq.order = append(wuwq.order, o...)
	return wuwq
}

// First returns the first WinUserWallet entity from the query.
// Returns a *NotFoundError when no WinUserWallet was found.
func (wuwq *WinUserWalletQuery) First(ctx context.Context) (*WinUserWallet, error) {
	nodes, err := wuwq.Limit(1).All(setContextOp(ctx, wuwq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{winuserwallet.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) FirstX(ctx context.Context) *WinUserWallet {
	node, err := wuwq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first WinUserWallet ID from the query.
// Returns a *NotFoundError when no WinUserWallet ID was found.
func (wuwq *WinUserWalletQuery) FirstID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wuwq.Limit(1).IDs(setContextOp(ctx, wuwq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{winuserwallet.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) FirstIDX(ctx context.Context) int32 {
	id, err := wuwq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single WinUserWallet entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one WinUserWallet entity is found.
// Returns a *NotFoundError when no WinUserWallet entities are found.
func (wuwq *WinUserWalletQuery) Only(ctx context.Context) (*WinUserWallet, error) {
	nodes, err := wuwq.Limit(2).All(setContextOp(ctx, wuwq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{winuserwallet.Label}
	default:
		return nil, &NotSingularError{winuserwallet.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) OnlyX(ctx context.Context) *WinUserWallet {
	node, err := wuwq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only WinUserWallet ID in the query.
// Returns a *NotSingularError when more than one WinUserWallet ID is found.
// Returns a *NotFoundError when no entities are found.
func (wuwq *WinUserWalletQuery) OnlyID(ctx context.Context) (id int32, err error) {
	var ids []int32
	if ids, err = wuwq.Limit(2).IDs(setContextOp(ctx, wuwq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{winuserwallet.Label}
	default:
		err = &NotSingularError{winuserwallet.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) OnlyIDX(ctx context.Context) int32 {
	id, err := wuwq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of WinUserWallets.
func (wuwq *WinUserWalletQuery) All(ctx context.Context) ([]*WinUserWallet, error) {
	ctx = setContextOp(ctx, wuwq.ctx, "All")
	if err := wuwq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*WinUserWallet, *WinUserWalletQuery]()
	return withInterceptors[[]*WinUserWallet](ctx, wuwq, qr, wuwq.inters)
}

// AllX is like All, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) AllX(ctx context.Context) []*WinUserWallet {
	nodes, err := wuwq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of WinUserWallet IDs.
func (wuwq *WinUserWalletQuery) IDs(ctx context.Context) (ids []int32, err error) {
	if wuwq.ctx.Unique == nil && wuwq.path != nil {
		wuwq.Unique(true)
	}
	ctx = setContextOp(ctx, wuwq.ctx, "IDs")
	if err = wuwq.Select(winuserwallet.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) IDsX(ctx context.Context) []int32 {
	ids, err := wuwq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (wuwq *WinUserWalletQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, wuwq.ctx, "Count")
	if err := wuwq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, wuwq, querierCount[*WinUserWalletQuery](), wuwq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) CountX(ctx context.Context) int {
	count, err := wuwq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (wuwq *WinUserWalletQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, wuwq.ctx, "Exist")
	switch _, err := wuwq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (wuwq *WinUserWalletQuery) ExistX(ctx context.Context) bool {
	exist, err := wuwq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the WinUserWalletQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (wuwq *WinUserWalletQuery) Clone() *WinUserWalletQuery {
	if wuwq == nil {
		return nil
	}
	return &WinUserWalletQuery{
		config:     wuwq.config,
		ctx:        wuwq.ctx.Clone(),
		order:      append([]winuserwallet.OrderOption{}, wuwq.order...),
		inters:     append([]Interceptor{}, wuwq.inters...),
		predicates: append([]predicate.WinUserWallet{}, wuwq.predicates...),
		// clone intermediate query.
		sql:  wuwq.sql.Clone(),
		path: wuwq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.WinUserWallet.Query().
//		GroupBy(winuserwallet.FieldUsername).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (wuwq *WinUserWalletQuery) GroupBy(field string, fields ...string) *WinUserWalletGroupBy {
	wuwq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &WinUserWalletGroupBy{build: wuwq}
	grbuild.flds = &wuwq.ctx.Fields
	grbuild.label = winuserwallet.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Username string `json:"username,omitempty"`
//	}
//
//	client.WinUserWallet.Query().
//		Select(winuserwallet.FieldUsername).
//		Scan(ctx, &v)
func (wuwq *WinUserWalletQuery) Select(fields ...string) *WinUserWalletSelect {
	wuwq.ctx.Fields = append(wuwq.ctx.Fields, fields...)
	sbuild := &WinUserWalletSelect{WinUserWalletQuery: wuwq}
	sbuild.label = winuserwallet.Label
	sbuild.flds, sbuild.scan = &wuwq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a WinUserWalletSelect configured with the given aggregations.
func (wuwq *WinUserWalletQuery) Aggregate(fns ...AggregateFunc) *WinUserWalletSelect {
	return wuwq.Select().Aggregate(fns...)
}

func (wuwq *WinUserWalletQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range wuwq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, wuwq); err != nil {
				return err
			}
		}
	}
	for _, f := range wuwq.ctx.Fields {
		if !winuserwallet.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if wuwq.path != nil {
		prev, err := wuwq.path(ctx)
		if err != nil {
			return err
		}
		wuwq.sql = prev
	}
	return nil
}

func (wuwq *WinUserWalletQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*WinUserWallet, error) {
	var (
		nodes = []*WinUserWallet{}
		_spec = wuwq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*WinUserWallet).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &WinUserWallet{config: wuwq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, wuwq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (wuwq *WinUserWalletQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := wuwq.querySpec()
	_spec.Node.Columns = wuwq.ctx.Fields
	if len(wuwq.ctx.Fields) > 0 {
		_spec.Unique = wuwq.ctx.Unique != nil && *wuwq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, wuwq.driver, _spec)
}

func (wuwq *WinUserWalletQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(winuserwallet.Table, winuserwallet.Columns, sqlgraph.NewFieldSpec(winuserwallet.FieldID, field.TypeInt32))
	_spec.From = wuwq.sql
	if unique := wuwq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if wuwq.path != nil {
		_spec.Unique = true
	}
	if fields := wuwq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, winuserwallet.FieldID)
		for i := range fields {
			if fields[i] != winuserwallet.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := wuwq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := wuwq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := wuwq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := wuwq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (wuwq *WinUserWalletQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(wuwq.driver.Dialect())
	t1 := builder.Table(winuserwallet.Table)
	columns := wuwq.ctx.Fields
	if len(columns) == 0 {
		columns = winuserwallet.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wuwq.sql != nil {
		selector = wuwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wuwq.ctx.Unique != nil && *wuwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wuwq.predicates {
		p(selector)
	}
	for _, p := range wuwq.order {
		p(selector)
	}
	if offset := wuwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wuwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

func (wuwq *WinUserWalletQuery) SqlQuery(ctx context.Context) (r1 string, r2 []any) {
	builder := sql.Dialect(wuwq.driver.Dialect())
	t1 := builder.Table(winuserwallet.Table)
	columns := wuwq.ctx.Fields
	if len(columns) == 0 {
		columns = winuserwallet.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if wuwq.sql != nil {
		selector = wuwq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if wuwq.ctx.Unique != nil && *wuwq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range wuwq.predicates {
		p(selector)
	}
	for _, p := range wuwq.order {
		p(selector)
	}
	if offset := wuwq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := wuwq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	r1, r2 = selector.Query()
	//r3 = make([]*WinUserWallet,0)

	return
}

// WinUserWalletGroupBy is the group-by builder for WinUserWallet entities.
type WinUserWalletGroupBy struct {
	selector
	build *WinUserWalletQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (wuwgb *WinUserWalletGroupBy) Aggregate(fns ...AggregateFunc) *WinUserWalletGroupBy {
	wuwgb.fns = append(wuwgb.fns, fns...)
	return wuwgb
}

// Scan applies the selector query and scans the result into the given value.
func (wuwgb *WinUserWalletGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wuwgb.build.ctx, "GroupBy")
	if err := wuwgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinUserWalletQuery, *WinUserWalletGroupBy](ctx, wuwgb.build, wuwgb, wuwgb.build.inters, v)
}

func (wuwgb *WinUserWalletGroupBy) sqlScan(ctx context.Context, root *WinUserWalletQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(wuwgb.fns))
	for _, fn := range wuwgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*wuwgb.flds)+len(wuwgb.fns))
		for _, f := range *wuwgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*wuwgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wuwgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// WinUserWalletSelect is the builder for selecting fields of WinUserWallet entities.
type WinUserWalletSelect struct {
	*WinUserWalletQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (wuws *WinUserWalletSelect) Aggregate(fns ...AggregateFunc) *WinUserWalletSelect {
	wuws.fns = append(wuws.fns, fns...)
	return wuws
}

// Scan applies the selector query and scans the result into the given value.
func (wuws *WinUserWalletSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, wuws.ctx, "Select")
	if err := wuws.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*WinUserWalletQuery, *WinUserWalletSelect](ctx, wuws.WinUserWalletQuery, wuws, wuws.inters, v)
}

func (wuws *WinUserWalletSelect) sqlScan(ctx context.Context, root *WinUserWalletQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(wuws.fns))
	for _, fn := range wuws.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*wuws.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := wuws.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
